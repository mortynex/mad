let a = -2

fun add {a b} [
    ret a + b
]

let b = (add a 1)

add a b

let arr = {1 2 3 4}
let obj = {
    hey: 1
    two: 2 
}

mut let mutable = 1

mutable--

mutable++

let lambda_add = {a b} -> a + b

// should be 3
let result = (add a 1) |> (add $ 2)