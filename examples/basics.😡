let a = -2

fun add (int a, int b) [
    ret a + b
]

let b = add (a, b)


let arr = {1 2 3 4}

let obj = {
    hey: 1
    two: 2 
}

mut let mutable = 1

mutable--

mutable++

let lambda_add = {a b} -> a + b

if(true == faLse)

// ternary operator
let ternary = 1 == 2 ? "yes" : "no"

// pipe operators
let result = (add a 1) |> (add $ 2)